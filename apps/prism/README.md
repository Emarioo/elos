**Prism** is the native compositor for **ELOS**. The OS does not depend on
the compositor but it does come packaged with the OS and will auto-start by default.

Which programs (if any) to auto-start can be changed in the *config* (@TODO where to find it on the system?).
Use *kernel console* (@TODO link to kernel console?) to modify the config.
