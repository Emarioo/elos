
Some design nomenclature stuff

# In kernel

**Kernel Component (or just Component)**
A distinct part of the kernel like PCI, CPU, ASYNC, DISK, PMEM which is responsible for a subsystem of the kernel.

**Kernel Module**
A loadable piece of code that runs in the kernel.

